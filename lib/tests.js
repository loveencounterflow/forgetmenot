// Generated by CoffeeScript 1.11.1
(function() {
  var CND, FMN, FS, PATH, badge, debug, echo, help, include, info, log, rpr, step, templates_home, test, test_data_home, urge, warn, whisper,
    indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'FORGETMENOT/TESTS';

  log = CND.get_logger('plain', badge);

  debug = CND.get_logger('debug', badge);

  info = CND.get_logger('info', badge);

  warn = CND.get_logger('warn', badge);

  help = CND.get_logger('help', badge);

  urge = CND.get_logger('urge', badge);

  whisper = CND.get_logger('whisper', badge);

  echo = CND.echo.bind(CND);

  test = require('guy-test');

  PATH = require('path');

  FS = require('fs');

  step = require('coffeenode-suspend').step;

  test_data_home = PATH.resolve(__dirname, '../test-data');

  templates_home = PATH.resolve(test_data_home, 'templates');

  FMN = require('..');

  this._prune = function() {
    var name, ref, value;
    ref = this;
    for (name in ref) {
      value = ref[name];
      if (name.startsWith('_')) {
        continue;
      }
      if (indexOf.call(include, name) < 0) {
        delete this[name];
      }
    }
    return null;
  };

  this._procure_test_files = function() {
    var byte_count, file_count, filename, i, len, ref, source_path, target_path;
    file_count = 0;
    ref = FS.readdirSync(templates_home);
    for (i = 0, len = ref.length; i < len; i++) {
      filename = ref[i];
      file_count += +1;
      source_path = PATH.resolve(templates_home, filename);
      target_path = PATH.resolve(test_data_home, filename);
      byte_count = this._copy_file_sync(source_path, target_path);
    }
    return whisper("copied " + file_count + " files");
  };

  this._copy_file_sync = function(source_path, target_path) {
    var source;
    FS.writeFileSync(target_path, source = FS.readFileSync(source_path));
    return source.length;
  };

  this._require_file = function(path) {

    /* Inhibit caching: */
    delete require['cache'][path];
    return require(path);
  };

  this._main = function() {
    return test(this, {
      'timeout': 5000
    });
  };

  this["create cache object (1)"] = function(T, done) {
    var i, len, matcher, probe, probes_and_matchers, ref, result;
    probes_and_matchers = [
      [
        {
          "update": false
        }, {
          "~isa": "FORGETMENOT/cache",
          "globs": [],
          "path": null,
          "files": {},
          "autosave": false
        }
      ], [
        {
          "update": true
        }, {
          "~isa": "FORGETMENOT/cache",
          "globs": [],
          "path": null,
          "files": {},
          "autosave": false
        }
      ], [
        {
          "autosave": true,
          "cache": "test-data/cache-1.json"
        }, {
          "~isa": "FORGETMENOT/cache",
          "globs": [],
          "path": "test-data/cache-1.json",
          "files": {},
          "autosave": true
        }
      ], [
        {
          "globs": "src/*"
        }, {
          "~isa": "FORGETMENOT/cache",
          "globs": ["src/*"],
          "path": null,
          "files": {},
          "autosave": false
        }
      ]
    ];
    for (i = 0, len = probes_and_matchers.length; i < len; i++) {
      ref = probes_and_matchers[i], probe = ref[0], matcher = ref[1];
      result = FMN.new_cache(probe);
      T.eq(result, matcher);
    }
    return done();
  };

  this["create cache object (2)"] = function(T, done) {
    step((function(_this) {
      return function*(resume) {
        var files, result, settings;
        settings = {
          globs: 'src/*'
        };
        result = (yield FMN.new_cache(settings, resume));
        files = result.files;
        T.eq(files[1101004112]['path'], 'src/main.coffee');
        T.eq(files[1467211317]['path'], 'src/tests.coffee');
        T.ok(CND.isa_number(files[1101004112]['checksum']));
        T.ok(CND.isa_number(files[1467211317]['checksum']));
        T.ok(FMN.DATE._looks_like_timestamp(files[1101004112]['timestamp']));
        T.ok(FMN.DATE._looks_like_timestamp(files[1467211317]['timestamp']));
        return done();
      };
    })(this));
    return null;
  };

  this["create cache object with path"] = function(T, done) {
    step((function(_this) {
      return function*(resume) {
        var files, result, settings;
        settings = {
          globs: 'src/*',
          cache: 'test-data/example-1.json'
        };
        result = (yield FMN.new_cache(settings, resume));
        debug('90988', result);
        files = result.files;
        T.eq(files[1101004112]['path'], 'src/main.coffee');
        T.eq(files[1467211317]['path'], 'src/tests.coffee');
        T.ok(CND.isa_number(files[1101004112]['checksum']));
        T.ok(CND.isa_number(files[1467211317]['checksum']));
        T.ok(FMN.DATE._looks_like_timestamp(files[1101004112]['timestamp']));
        T.ok(FMN.DATE._looks_like_timestamp(files[1467211317]['timestamp']));
        return done();
      };
    })(this));
    return null;
  };

  if (module.parent == null) {
    include = ["create cache object (1)", "create cache object (2)", "create cache object with path"];
    this._prune();
    this._main();
  }

}).call(this);

//# sourceMappingURL=tests.js.map
